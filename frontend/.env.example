# Environment variables consumed by the Angular frontend (injected at runtime/build by orchestrator)
# PUBLIC_INTERFACE
# Base URL for the Recipes API (e.g., https://api.example.com). If empty, the app will use in-memory mock data.
NG_APP_API_BASE=
